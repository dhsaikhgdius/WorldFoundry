# Upstream provenance — VMem (`vmem_runtime`)

Vendored / mirrored from [`runjiali-rl/vmem`](https://github.com/runjiali-rl/vmem).

Pinned revision: `39291e4f272f6b4f270691d930926ab5930f942e` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/vmem.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/vmem/` (outside this `*_runtime` tree).
