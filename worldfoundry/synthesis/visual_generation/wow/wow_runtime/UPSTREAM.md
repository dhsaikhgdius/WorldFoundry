# Upstream provenance — WoW (`wow_runtime`)

Vendored / mirrored from [`wow-world-model/wow-world-model`](https://github.com/wow-world-model/wow-world-model).

Pinned revision: `08b010597b69b5e1fde5f69ba44da85aff05bf62` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/wow.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/wow/` (outside this `*_runtime` tree).
