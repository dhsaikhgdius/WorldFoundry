# Upstream provenance — MagicWorld (`magic_world_runtime`)

Vendored / mirrored from [`vivoCameraResearch/Magic-World`](https://github.com/vivoCameraResearch/Magic-World).

Pinned revision: `a378d67d1b803db4268340fcc130c98a243ad9a8` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/magicworld.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/magic_world/` (outside this `*_runtime` tree).
