# Upstream provenance — TesserAct (`tesseract_runtime`)

Vendored / mirrored from [`UMass-Embodied-AGI/TesserAct`](https://github.com/UMass-Embodied-AGI/TesserAct).

Pinned revision: `d2fe4b1d509e54a3cf95da40ec6e232b68361d07` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/tesseract.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/tesseract/` (outside this `*_runtime` tree).
