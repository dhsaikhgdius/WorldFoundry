# Upstream provenance — MoVerse (`moverse_runtime`)

Vendored from [`Orange-3DV-Team/MoVerse`](https://github.com/Orange-3DV-Team/MoVerse)
at the revision recorded in `worldfoundry/data/models/runtime/profiles/moverse.yaml`
(`source_repos[0].revision`, currently `ae146303a826dfa7af2fea81fc492c9bc39651e8`).

## License status

The public upstream tree has **no root-level LICENSE** at the pinned revision.
WorldFoundry catalogs this as `license: not_found` /
`code: public_without_root_license`. Presence of this checkout is **not** an
open-source grant — review before redistribution or commercial use.

`LICENSE.NOTICE` in this directory records that fact for packaging scanners
that expect a license-like artifact next to every packaged `*_runtime`.

## Related components

Depth Anything 3 and other shared WorldFoundry base models used by MoVerse are
not vendored under this directory; see the moverse runtime profile checkpoints.
