# Upstream provenance — Matrix-Game 3 (`matrix_game_3_runtime`)

Vendored / mirrored from [`SkyworkAI/Matrix-Game`](https://github.com/SkyworkAI/Matrix-Game).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/matrix-game-3.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/matrix_game/` (outside this `*_runtime` tree).
