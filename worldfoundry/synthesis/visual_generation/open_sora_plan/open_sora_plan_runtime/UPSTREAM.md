# Upstream provenance — Open-Sora-Plan (`open_sora_plan_runtime`)

Vendored / mirrored from [`PKU-YuanGroup/Open-Sora-Plan`](https://github.com/PKU-YuanGroup/Open-Sora-Plan).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/open-sora-plan.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/open_sora_plan/` (outside this `*_runtime` tree).
