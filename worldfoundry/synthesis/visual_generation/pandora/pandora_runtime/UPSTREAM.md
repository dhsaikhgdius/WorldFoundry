# Upstream provenance — Pandora (`pandora_runtime`)

Vendored / mirrored from [`maitrix-org/Pandora`](https://github.com/maitrix-org/Pandora).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/pandora.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/pandora/` (outside this `*_runtime` tree).
