# Upstream provenance — VideoX-Fun (`video_x_fun_runtime`)

Vendored / mirrored from [`aigc-apps/VideoX-Fun`](https://github.com/aigc-apps/VideoX-Fun).

Pinned revision: `403f1f7b78dcafccc4f6606dda4ec28e16e667dc` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/wan22-fun-5b-cam.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/video_x_fun/` (outside this `*_runtime` tree).
