# Upstream provenance — Genie-Envisioner (`genie_envisioner_runtime`)

Vendored / mirrored from [`AgibotTech/Genie-Envisioner`](https://github.com/AgibotTech/Genie-Envisioner).

Pinned revision: `65ab2fa2191f74d213a3447013f5ff90b55dcc3d`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/genie_envisioner/` (outside this `*_runtime` tree).
