# Upstream provenance — UniAnimate-DiT (`unianimate_dit_runtime`)

Vendored / mirrored from [`ali-vilab/unianimate-dit`](https://github.com/ali-vilab/unianimate-dit).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/unianimate-dit.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/unianimate_dit/` (outside this `*_runtime` tree).
