# Upstream provenance — Spatia (`spatia_runtime`)

Vendored / mirrored from [`ZhaoJingjing713/Spatia`](https://github.com/ZhaoJingjing713/Spatia).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/spatia.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/spatia/` (outside this `*_runtime` tree).
