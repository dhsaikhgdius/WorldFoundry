# Upstream provenance — minWM (`minwm_runtime`)

Vendored / mirrored from [`shengshu-ai/minWM`](https://github.com/shengshu-ai/minWM).

Pinned revision: `df522a26cd4409d3e3e8f269cc98eac069b5df47` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/minwm-wan-action2v.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/minwm/` (outside this `*_runtime` tree).
