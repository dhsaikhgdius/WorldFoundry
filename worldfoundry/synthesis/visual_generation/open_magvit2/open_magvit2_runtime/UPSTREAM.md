# Upstream provenance — Open-MAGVIT2 (`open_magvit2_runtime`)

Vendored / mirrored from [`TencentARC/Open-MAGVIT2`](https://github.com/TencentARC/Open-MAGVIT2).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/open-magvit2.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/open_magvit2/` (outside this `*_runtime` tree).
