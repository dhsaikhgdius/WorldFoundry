# Upstream provenance — VerseCrafter (`versecrafter_runtime`)

Vendored / mirrored from [`TencentARC/VerseCrafter`](https://github.com/TencentARC/VerseCrafter).

Pinned revision: `008693b52aa74367afb34d183046fecf88100bdc` (see profile `source_repos`).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/versecrafter.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/versecrafter/` (outside this `*_runtime` tree).
