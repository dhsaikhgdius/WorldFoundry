# Upstream provenance — Warp as History (`warp_as_history_runtime`)

Vendored / mirrored from [`yyfz/Warp-as-History`](https://github.com/yyfz/Warp-as-History).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/warp-as-history.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/synthesis/visual_generation/warp_as_history/` (outside this `*_runtime` tree).
