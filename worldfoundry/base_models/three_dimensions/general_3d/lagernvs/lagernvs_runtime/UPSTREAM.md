# Upstream provenance — LagerNVS (`lagernvs_runtime`)

Vendored / mirrored from [`facebookresearch/lagernvs`](https://github.com/facebookresearch/lagernvs).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/lagernvs.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/base_models/three_dimensions/general_3d/lagernvs/` (outside this `*_runtime` tree).
