# Upstream provenance — Splatt3r (`splatt3r_runtime`)

Vendored / mirrored from [`btsmart/splatt3r`](https://github.com/btsmart/splatt3r).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/splatt3r.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/base_models/three_dimensions/general_3d/splatt3r/` (outside this `*_runtime` tree).
