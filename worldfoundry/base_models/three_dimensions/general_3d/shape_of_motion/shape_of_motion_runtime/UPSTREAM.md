# Upstream provenance — Shape of Motion (`shape_of_motion_runtime`)

Vendored / mirrored from [`vye16/shape-of-motion`](https://github.com/vye16/shape-of-motion).

Revision pins (when present) live in `worldfoundry/data/models/runtime/profiles/shape-of-motion.yaml`.

## License status

No root-level LICENSE was found beside this checkout_only runtime tree at the
time of the SY-06 provenance pass. `LICENSE.NOTICE` records that gap and is
**not** a license grant. Prefer upstream SPDX terms when a LICENSE appears in
a future sync.

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/base_models/three_dimensions/general_3d/shape_of_motion/` (outside this `*_runtime` tree).
