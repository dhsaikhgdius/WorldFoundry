# Upstream provenance — HPSv3 (`hpsv3_runtime`)

Vendored / mirrored from [`MizzenAI/HPSv3`](https://github.com/MizzenAI/HPSv3).

## License status

A root-level `LICENSE` file is present beside this checkout_only runtime tree
(see that file for SPDX / copyright terms).

## Local WorldFoundry adapters

Launch / conda / checkpoint resolution stay in the sibling WorldFoundry adapter
modules under `worldfoundry/base_models/perception_core/video_quality/` (outside this `*_runtime` tree).
