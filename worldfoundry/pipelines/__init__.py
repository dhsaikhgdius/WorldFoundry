"""WorldFoundry pipeline binding packages.

This package is intentionally a namespace-friendly root so setuptools
``find`` with ``namespaces = true`` ships ``worldfoundry.pipelines.*``
binding modules in the wheel (SY-01).
"""
