VERDICT: CONFIRMED

# V1 对抗验证报告（逻辑审计）— T04 定理 A/B、9/65、命题 P2

审计员：V1（逻辑审计，独立于 V2 盲复现、V3 证书与文献审计）。
方法：带敌意逐项审查。所有复核均独立实现（不 import 被审代码）：
- `v1_audit_numeric.py` — 纯 Fraction 精确枚举（自行重新推导临界时刻定理后实现，另配全网格超集交叉核对）；
- `v1_audit_symbolic.py` — sympy 从零重推全部 22 条证书不等式与全部 14 条 witness 不等式。
两脚本最终输出均为 ALL CHECKS PASSED（0 失败）。以下逐项回答审计清单。

---

## 1. 定义与规约 — 严密

- **ML 定义**：ML(v)=sup_{t∈R} min_i ||t·v_i||，f(t)=min_i||t v_i|| 连续、分段线性、以 1 为周期（速度为整数）；且 f(1−t)=f(t)（||−x||=||x||）。故 sup_R f = max_{[0,1]} f = max_{[0,1/2]} f，归约合法。
- **sup 是否达到**：f 连续、[0,1] 紧 ⟹ sup 达到。且下界论证根本不依赖达到性：sup ≥ f(t*) 对任意单点 t* 平凡成立（sup 的定义），witness 逻辑无循环、无缝隙。
- **口径**：k=7 指 7 个非零互异速度 + 1 个静止观察者 = 8 名跑者，LRC 界 1/(k+1)=1/8。与其 §2 表内部一致（{1,…,7} 紧例 ML=1/8；k=4 紧例 {1,3,4,7} ML=1/5），也与 Goddyn–Wong / Fan–Sun 的惯用口径一致。我独立复核 GW 两个 k=7 紧例 ML(1,2,3,4,5,7,12)=ML(1,4,5,6,7,11,13)=1/8 ✓。（引文真实性归 V3。）
- **临界时刻定理（其 §0，等号引理 3 的基础）**：我独立重证：f 各线性段斜率 ±v_i ≠ 0 ⟹ 全局 max 在断点处、左段升右段降 ⟹ t* 为帐顶 (2a+1)/(2v_i) 或升降交点 (a+b)/(v_i+v_j)，故 ML = max{f(c/d): d ∈ {2v_i}∪{v_i+v_j}}。成立。我的独立实现与"全网格 d ≤ 2·max(v) 超集"实现在 12 个实例上完全一致。

## 2. 覆盖法有效性 — 成立，端点/相切无缝隙

- **推理链**：B(v,m)=[(m−α)/v,(m+α)/v] ⊆ {t: ||vt||≤α}（闭带，无条件成立）；链条件 lo₁≤0、lo_{j+1}≤hi_j、hi_last≥1/2 ⟹（对闭区间的标准归纳）∪ 带 ⊇ [0,1/2] ⟹ 对称+周期 ⟹ ∀t∈R: min_i ||v_i t|| ≤ α ⟹ sup ≤ α。逻辑完备。
- **相切点（P≡0 链节）**：A 的 hiB(w,r+2)=loB(5,1)、B 的 hiB(7,1)=loB(w,r+4) 为恒等相切（sympy 验证恒等式成立），相切点**恰为 witness t***。因带取闭区间，切点被两带同时包含，无缝隙；且该点 f(t*)=α 与上界 ≤α 相容（sup 在此被"夹住"，结构自洽）。若用开带此处会漏一个点——他们用的是闭带，正确。
- **α 代入**：证书直接用目标值 α(r)=(r+2)/(6r+17)（B: (r+4)/(6r+31)）本身，非严格小量，结论正是所需的 ML ≤ α。无"ε 松弛"问题。
- **独立区间并核查**：我用 Fraction 对 r∈{3,4,7,9,12,30,101} 直接构造证书带、排序合并区间，均精确覆盖 [0,1/2] ✓（不依赖链归纳的第二种验法）。

## 3. 多项式论证 — 全部 22 条重推一致；r 范围衔接无漏格

- 我用 sympy **从零重推全部链接不等式**（A 9 条 + B 13 条，超过要求的 5 条）：每条化为整系数多项式 P(r)≥0；与 certificates_AB.txt 逐条比对，**全部等价**——证书多项式 = 我的多项式 ×(6r+17)（或 ×(6r+31)，即未约去的公共正分母因子），差一个显式正因子，不影响符号。抽样对照（A）：链2 我得 2r−1，证书 12r²+28r−17=(6r+17)(2r−1)✓；链5 我得 r−3，证书 6r²−r−51=(6r+17)(r−3)✓。全部 22 条同法验证 ✓。
- **s=r−7 代换逻辑**：p(s+7) 各系数 ≥0 ⟹ p 在实数 s≥0 即 r≥7 上非负——充分判据，健全（sound）；判据失败时其代码显式拒绝（无不健全回退）。我另用 sympy real_roots 求出每条链节的**精确成立门槛**：A 与 B 均为 r≥3（最紧两条 P=r−3 在 r=3 相切为零，闭带下相切仍覆盖），其余链节在 r≥1 甚至全体实数成立。故 r≥7 的证书是**保守而正确**的。
- **衔接无漏格**：上界符号证明覆盖整数 r≥7；引理 3 精确枚举覆盖 1≤r≤12；并集 = 全体 r≥1，且 7..12 双重覆盖。我独立复核等号到 **r≤16 并抽查 r=20,50,100,200**（远超其 12 的窗口，深入"纯符号区"）：全部 ML=α(r) 精确成立 ✓。链条确实在 r=1,2 失效（实际间隙已展示于脚本输出），恰好落在枚举窗口内——分工必要且充分。

## 4. 大速度"单一带覆盖主块" — 重点攻击后确认无 off-by-one

- w 的坏集在 [0,1] 内确有 ~w 个周期段，但覆盖是充分性论证：只需其中**一条**带 B(w, m(r)) 插对位置即可，其余段无害（多余坏带只会多覆盖）。
- **m(r) 是否对**：m=r+2（A）/ r+4（B）为整数值仿射多项式；核心恒等式 (6r+12)(r+3)=(6r+17)(r+2)+(r+2) 与 (6r+24)(r+5)=(6r+31)(r+4)−(r+4) 经 sympy 展开恒等 ✓，即 w·t* = (r+2)+α·q/q（A，从上方偏离整数 r+2 恰 α）/ (r+4)−α（B，从下方）。无 off-by-one。
- **宽度足够吗**：主间隙 [hiB(7,1), loB(5,1)]（A）长 2/(7q)，w 带宽 2α/w = 1/(3q) > 2/(7q)（1/3>2/7），代数上恰能塞入且右端精确相切于 t*；B 同理（间隙 3/(11q) < 1/(3q)，还有 B(11,2) 接力）。两条"插入不等式"即链 2/3 与链 3/4，已在第 3 项中逐条符号验证 ✓。
- **边界探针（确认无过度外推）**：家族公式若外推到 r=0，A 给 2/17 < 1/8，将违反 LRC——实测 ML(1,2,3,4,5,7,12)=1/8（GW 紧例）；B 的 r=0 端点实测 ML(1,3,4,5,7,11,24)=2/15 ≠ 4/31。定理声明从 r=1 起，恰好不含这两点 ✓ 无 overclaim。

## 5. 9/65 与命题 P2 — 确认

- **9/65**：我的独立 Fraction 实现（临界分母网格 + 全网格双验）：ML(2,7,9,11,13,20,54) = 9/65 精确成立，witness t=11/65 处 f=9/65 ✓（11·11=121≡56, 65−56=9；54·11=594≡9 (mod 65)，分母对 11+54=65 ✓）。s=9, j=65−63=2 ✓ 新 j=2 谱点成立。
- **其余 6 个 j≥2 实例**（P1 的数据基础）全部独立复核：5/22, 5/33, 8/51, 3/23×2, 5/37 精确一致，且每例存在 v_a+v_b=q 的分母对 ✓。P1 明确标注"一般证明未证"，无 overclaim。
- **P2 逐步审查**：设 ML=s/(ks+j) 最简、<1/k、k≤9。(i) j≥1：s/(ks+j)<1/k ⟺ ks<ks+j ⟺ j>0（分母正，叉乘合法）✓；(ii) j≤s：LRC_k ⟹ ML≥1/(k+1) ⟺ s(k+1)≥ks+j ⟺ s≥j ✓；(iii) j=s ⟹ ML=1/(k+1)，而 gcd(s, s(k+1))=s，最简 ⟹ s=1 ⟹ (s,j)=(1,1) ✓。证明初等且完备。**条件依赖**：LRC 在 k≤9 已证是文献命题（其 §1 引 Rosenfeld 2025 / Trakulthongchai 2026），归 V3 核实；若该范围有变，P2 结论范围随之缩放，但条件式陈述本身无逻辑瑕疵。他们也自注"可能隐含于文献"，新颖性主张审慎。
- **推论 6 元锚集**：独立复核 ML(1,2,3,4,5,7)=ML(1,3,4,5,7,11)=1/6（witness 1/6）✓。

## 6. 复现与代码卫生（次要观察，非缺陷）

- 只读复跑 cover_prover.py 与 witness_prover.py：输出与 findings/证书完全一致（8 带 + 11 带、下界 r≥1、上界 r≥7）。
- witness_prover.py 第 63 行有一句冗余死代码（c2 先误算后立即被正确值覆盖），不影响健全性；证书发现流程（数值样本 r∈{7,13,50,200} 拟合 m(r)）只是启发式来源，最终验证与发现方式无关——结构正确。
- 证书文本 P(s+7) 展开系数我亦抽样手算核对（如 12r²+28r−17 → 12s²+196s+767）✓。

## 结论

定理 A、定理 B 的证明结构（witness 下界 ∀r≥1 + 坏带覆盖上界 r≥7 + 精确枚举 1≤r≤12）逻辑完备，全部机器判据健全且被我独立重推/重算复现；9/65 新谱点与 P2 成立。未发现任何逻辑漏洞。唯一外部依赖（P2 前提"LRC 已证至 k≤9"及各引文真实性）属 V3 职权，不影响本审计范围内的 CONFIRMED 判定。

### 审计产物
- `v1_audit_numeric.py`（独立精确枚举：家族 r≤16 及 20/50/100/200 抽查、9/65、7 个 j≥2 实例、锚集、证书带区间并覆盖核查、r=1,2 失效间隙展示、r=0 边界探针）— ALL PASSED
- `v1_audit_symbolic.py`（sympy 重推 22 条链接不等式 + 14 条 witness 不等式 + 相切恒等式，含每条的精确成立门槛 r≥3）— ALL PASSED
