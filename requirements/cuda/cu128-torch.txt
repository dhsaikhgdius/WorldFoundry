# Torch wheel constraints for the cu128 CUDA bucket (plan I-03).
#
# Stubs for dry-run / drift checks. Bounds must match
# ``worldfoundry.runtime.cuda_tiers.TIER_TORCH_SPECS``.
# NOT full lockfiles — see requirements/lock/ for I-05.

torch>=2.7,<2.12.0
torchvision>=0.22,<0.27.0
torchaudio>=2.7,<2.12.0
