# Torch wheel constraints for the cu121 CUDA bucket (plan I-03).
#
# Stubs for dry-run / drift checks. Bounds must match
# ``worldfoundry.runtime.cuda_tiers.TIER_TORCH_SPECS`` and what
# https://download.pytorch.org/whl/cu121 actually publishes (torch<=2.5.x).
# NOT full lockfiles — see requirements/lock/ for I-05.

torch>=2.4,<2.6
torchvision>=0.19,<0.21
torchaudio>=2.4,<2.6
