name: Deploy Docs

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/deploy-docs.yml"
      - "docs/**"
      - "scripts/docs/build.sh"
      # Site content is partly generated from catalog / API surfaces.
      - "worldfoundry/data/models/**"
      - "worldfoundry/data/benchmarks/**"
      - "worldfoundry/evaluation/api/**"
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: github-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_BASE_PATH: /${{ github.event.repository.name }}
      # Prefer the owning org/user rather than a hard-coded Pages host.
      NEXT_PUBLIC_SITE_URL: https://${{ github.repository_owner }}.github.io
      # Pin demo media to the repository default branch so PR/ref builds do not
      # 404 against floating github.ref_name paths.
      NEXT_PUBLIC_DEMO_ASSET_BASE_URL: https://media.githubusercontent.com/media/${{ github.repository }}/${{ github.event.repository.default_branch }}/docs/fumadocs/public

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: docs/fumadocs/package-lock.json

      - name: Configure GitHub Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Build Fumadocs site
        run: bash scripts/docs/build.sh --skip-bootstrap

      - name: Preserve Next.js assets
        run: |
          rm -rf docs/fumadocs/out/demos
          touch docs/fumadocs/out/.nojekyll

      - name: Upload GitHub Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs/fumadocs/out

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
